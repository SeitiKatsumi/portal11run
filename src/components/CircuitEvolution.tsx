"use client";
import { useEffect,useState } from 'react';
import { ResponsiveContainer,LineChart,Line,XAxis,YAxis,Tooltip,Legend,CartesianGrid } from 'recharts';
import type { circuitEvolution } from '@/lib/virtual-circuit-core';
import { CIRCUIT_ABSOLUTE,CIRCUIT_MONTHS,CIRCUIT_BIMONTHS } from '@/lib/virtual-circuit-schedule';
import styles from './CircuitWorkspace.module.css';
type Evolution=ReturnType<typeof circuitEvolution>[number];
const colors=['#f97316','#2563eb','#0d9488','#9333ea','#e11d48'];
const time=(ms:number)=>`${Math.floor(ms/60000)}:${String(Math.floor(ms%60000/1000)).padStart(2,'0')}.${String(Math.round(ms%1000/10)).padStart(2,'0')}`;
export function CircuitEvolution(){
 const [ranking,setRanking]=useState<Evolution[]>([]);const [period,setPeriod]=useState('edition');const [age,setAge]=useState('');const [gender,setGender]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(true);
 const periods=[{...CIRCUIT_ABSOLUTE,id:'edition'},...CIRCUIT_MONTHS,...CIRCUIT_BIMONTHS];const active=periods.find(p=>p.id===period)!;
 useEffect(()=>{const controller=new AbortController();setLoading(true);setError('');const q=new URLSearchParams({mode:'evolution',start:active.start,end:active.end,age,gender});fetch(`/api/circuito-virtual/ranking?${q}`,{signal:controller.signal,cache:'no-store'}).then(r=>{if(!r.ok)throw new Error('Não foi possível carregar a evolução.');return r.json();}).then(j=>setRanking(j.ranking)).catch(e=>{if(e.name!=='AbortError')setError(e.message);}).finally(()=>{if(!controller.signal.aborted)setLoading(false);});return()=>controller.abort();},[active.start,active.end,age,gender]);
 const top=ranking.filter(a=>(a.percent??0)>0).slice(0,5);
 const dates=[...new Set(top.flatMap(a=>a.history.map(m=>m.activityDate)))].sort();
 // Keep every mark, including several results on the same date.
 const data=dates.flatMap(date=>{const count=Math.max(...top.map(a=>a.history.filter(m=>m.activityDate===date).length));return Array.from({length:count},(_,i)=>Object.fromEntries([['date',date],...top.map(a=>[String(a.athleteNumber),a.history.filter(m=>m.activityDate===date)[i]?.timeMs??null])]));});
 return <section className={styles.workspace} id="evolucao"><header><span>Seu progresso também merece destaque</span><h2>Ranking de melhor evolução</h2><p>Da primeira marca à melhor posterior: a redução percentual do tempo de cada atleta. 5:00 → 4:30 representa 10% de evolução.</p></header><div className={styles.tabs}><label>Período<select value={period} onChange={e=>setPeriod(e.target.value)}>{periods.map(p=><option key={p.id} value={p.id}>{p.label}</option>)}</select></label><label>Idade em 2026<select value={age} onChange={e=>setAge(e.target.value)}><option value="">Todas</option>{[9,10,11,12,13].map(a=><option key={a}>{a}</option>)}</select></label><label>Gênero<select value={gender} onChange={e=>setGender(e.target.value)}><option value="">Todos</option><option value="FEMALE">Feminino</option><option value="MALE">Masculino</option></select></label></div>{error?<p role="alert">{error}</p>:loading?<p role="status">Carregando evolução…</p>:<>{top.length>0?<><h3>As cinco maiores evoluções</h3><div style={{width:'100%',height:340,minWidth:0}} aria-label="Gráfico de tempos por data; valores disponíveis na tabela abaixo"><ResponsiveContainer width="100%" height="100%"><LineChart data={data} margin={{top:15,right:20,left:0,bottom:10}}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="date" tickFormatter={s=>String(s).slice(5).split('-').reverse().join('/')}/><YAxis width={85} tickFormatter={time} domain={['auto','auto']}/><Tooltip formatter={v=>time(Number(v))} labelFormatter={v=>String(v).split('-').reverse().join('/')}/><Legend/>{top.map((a,i)=><Line key={a.athleteNumber} dataKey={String(a.athleteNumber)} name={`#${a.athleteNumber} ${a.publicName} · ${a.percent!.toFixed(2)}%`} stroke={colors[i]} strokeWidth={2} connectNulls dot={{r:4}} isAnimationActive={false}/>)}</LineChart></ResponsiveContainer></div><details><summary>Ver todos os valores do gráfico</summary><div className={styles.scroll}><table><thead><tr><th>Atleta</th><th>Data</th><th>Marca</th><th>Evolução</th></tr></thead><tbody>{top.flatMap(a=>a.history.map(m=><tr key={`${a.athleteNumber}:${m.id}`}><td>#{a.athleteNumber} {a.publicName}</td><td>{m.activityDate.split('-').reverse().join('/')}</td><td>{time(m.timeMs)}</td><td>{a.percent!.toFixed(2)}%</td></tr>))}</tbody></table></div></details></>:<p>Ainda não há evolução positiva comprovada neste período.</p>}<div className={styles.scroll}><table><thead><tr><th>Posição</th><th>Atleta</th><th>Categoria</th><th>Primeira marca</th><th>Melhor posterior</th><th>Evolução</th></tr></thead><tbody>{ranking.filter(a=>a.percent!==null).map((a,i)=><tr key={a.athleteNumber}><td>{a.percent===null?'—':i+1}</td><td>#{a.athleteNumber} {a.publicName}</td><td>Sub {a.categoryAge+1} · {a.gender==='FEMALE'?'F':'M'}</td><td>{a.firstTime}</td><td>{a.percent===null?'—':a.bestTime}</td><td>{a.percent===null?'Sem comparação':`${a.percent.toFixed(2)}%`}</td></tr>)}</tbody></table></div>{ranking.some(a=>a.percent===null)&&<details><summary>Atletas aguardando marcas em outra data ({ranking.filter(a=>a.percent===null).length})</summary><div className={styles.cards}>{ranking.filter(a=>a.percent===null).map(a=><p key={a.athleteNumber}>#{a.athleteNumber} {a.publicName} · {a.firstTime} · Sem comparação</p>)}</div></details>}<p>São necessárias marcas aprovadas em duas datas distintas. Sem redução: 0%. Esta classificação não altera a premiação por melhor tempo.</p></>}</section>;
}
