version: "3.8"

services:
  portal11run:
    build: .
    container_name: portal11run
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - SQLITE_PATH=/data/portal11run.sqlite
    volumes:
      - ./data:/data
    restart: always
